import "@eq.*"
import "@to_string.*"

pub object Bool => [Eq[Bool], ToString]: class =
	pub @eq(self, y) => self == y end

	pub @ne(self, y) => self != y end

	pub @to_int self =>
		if self do 1 
		else 0
		end
	end

	pub @to_string self =>
		if self do "True"
		else "False"
		end
	end
end
